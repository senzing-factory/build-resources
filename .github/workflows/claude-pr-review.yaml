name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize]

permissions: {}

jobs:
  review:
    uses: senzing-factory/build-resources/.github/workflows/claude-pull-request-review.yaml@227-skern
